var keyMirror = require('react/lib/keyMirror');

module.exports = keyMirror({
    WEBAPI_GET: null,
    WEPAPI_SET: null,
    WEBAPI_SETSCALE: null
});

// WEPAPI_SET bad name, also WEBAPI_SETSCALE
// should have one store for data
// and one store for manipulation of data
